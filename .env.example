# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=competencias_db
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# JWT
JWT_SECRET=cambia_este_secreto_por_uno_seguro_en_produccion
JWT_EXPIRES_IN=8h

# Servidor
PORT=3001
NODE_ENV=development

# CORS - origen del frontend
FRONTEND_URL=http://localhost:5173

# SSO (configurar según proveedor: Okta, Azure AD, Google, etc.)
# SSO_ISSUER=https://tu-proveedor.okta.com
# SSO_CLIENT_ID=xxx
# SSO_CLIENT_SECRET=xxx
